name: Build and Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      # Generate short-lived token from GitHub App
      - name: Generate GitHub App token
        uses: actions/create-github-app-token@v1
        id: app-token
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: CycoAi  # Target organization with packages
          repositories: cycodum-oauth2-client  # Explicitly request access to package repo
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
      
      # Authenticate to GitHub Packages (CRITICAL STEP!)
      - name: Authenticate to GitHub Packages
        run: |
          dotnet nuget update source CycoAi-GitHub \
            --username github \
            --password ${{ steps.app-token.outputs.token }} \
            --store-password-in-clear-text
      
      - name: Restore packages
        run: dotnet restore
      
      - name: Build
        run: dotnet build --no-restore --configuration Release
      
      - name: Verify sample compiles
        run: |
          echo "✅ Sample built successfully!"
          echo "📦 Package CycoAi.OAuth2.Client restored from CycoAi org"
          echo "🎉 Cross-organization package access working!"
